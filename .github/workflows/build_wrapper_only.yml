name: Compilador wrapper.tzst NDK Híbrido
on: workflow_dispatch
jobs:
  build-mali:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Instalar Herramientas Base
        run: |
          sudo apt-get update && sudo apt-get install -y meson ninja-build git zstd tar pkg-config glslang-tools python3-mako
          pip3 install mako ply

      - name: 2. Checkout de tu Repositorio (Carga stubs .sh)
        uses: actions/checkout@v4

      - name: 3. Descargar Código de Leegao
        uses: actions/checkout@v4
        with:
          repository: 'leegao/bionic-vulkan-wrapper'
          path: 'wrapper_src'
          fetch-depth: 1

      - name: 4. Setup Android NDK
        uses: android-actions/setup-android@v3

      - name: 5. Instalar NDK de Fábrica
        run: ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager --install "ndk;25.2.9519653"

      - name: 6. Ejecutar Scripts e Iniciar Compilación Dual Real
        run: |
          chmod +x setup_c_stubs.sh setup_stubs.sh
          ./setup_c_stubs.sh
          ./setup_stubs.sh

          cd wrapper_src
          
          # PARCHE FÍSICO LOCAL INMUNE: Creamos los archivos puente reales en la carpeta include/
          # para obligar a Clang a inyectar las funciones sin importar las limpieces de Meson
          echo -e '#ifndef LOCAL_ZSTD_H\n#define LOCAL_ZSTD_H\n#endif' > include/zstd.h
          echo -e '#ifndef LOCAL_ZLIB_H\n#define LOCAL_ZLIB_H\n#endif' > include/zlib.h
          
          cat << 'EOF' > include/xf86drm.h
          #ifndef _XF86DRM_H
          #define _XF86DRM_H
          #include <stdint.h>
          #include <stdbool.h>
          #define DRM_NODE_RENDER 0
          typedef struct _drmDevice { uint32_t available_nodes; } drmDevice, *drmDevicePtr;
          int drmIoctl(int fd, unsigned long request, void *arg);
          int drmGetCap(int fd, uint64_t capability, uint64_t *value);
          int drmPrimeFDToHandle(int fd, int prime_fd, uint32_t *handle);
          int drmSyncobjCreate(int fd, uint32_t flags, uint32_t *handle);
          int drmSyncobjDestroy(int fd, uint32_t handle);
          int drmSyncobjReset(int fd, const uint32_t *handles, uint32_t num_handles);
          int drmSyncobjSignal(int fd, const uint32_t *handles, uint32_t num_handles);
          int drmSyncobjWait(int fd, uint32_t *handles, uint32_t num_handles, int64_t timeout_nsec, uint32_t flags, uint32_t *first_signaled);
          int drmSyncobjExportSyncFile(int fd, uint32_t handle, int *map_fd);
          int drmSyncobjImportSyncFile(int fd, uint32_t handle, int map_fd);
          int drmSyncobjFDToHandle(int fd, int map_fd, uint32_t *handle);
          int drmSyncobjHandleToFD(int fd, uint32_t handle, int *map_fd);
          int drmSyncobjTimelineSignal(int fd, const uint32_t *handles, const uint64_t *points, uint32_t num_handles);
          int drmSyncobjTimelineWait(int fd, uint32_t *handles, uint64_t *points, uint32_t num_handles, int64_t timeout_nsec, uint32_t flags, uint32_t *first_signaled);
          int drmSyncobjQuery(int fd, const uint32_t *handles, uint64_t *points, uint32_t num_handles);
          int drmSyncobjTransfer(int fd, uint32_t dst_handle, uint64_t dst_point, uint32_t src_handle, uint64_t src_point, uint32_t flags);
          #endif
          EOF

          # PARCHE MAESTRO EN CALIENTE: Inyectamos el puente de stubs al principio de los archivos utilitarios
          sed -i '1i #include <vk_pc_stubs.h>' src/util/compress.c || true
          sed -i '1i #include <vk_pc_stubs.h>' src/util/crc32.c || true
          sed -i '1i #include <vk_pc_stubs.h>' src/vulkan/runtime/vk_drm_syncobj.c || true

          # Enrutamiento automático universal hacia las carpetas físicas internas del Unisoc T618
          find . -type f \( -name "*.c" -o -name "*.h" -o -name "*.cpp" \) | while read -r f; do
            if grep -q '"libvulkan.so"' "$f"; then
              sed -i 's/"libvulkan.so"/(sizeof(void*) == 8 ? "\/system\/lib64\/libvulkan.so" : "\/system\/lib\/libvulkan.so")/g' "$f" || true
            fi
          done

          sed -i '1i glslang_quiet = []\nglslang_depfile = []' src/vulkan/wrapper/meson.build || true
          echo "" > src/vulkan/wsi/wsi_common_ahardware_buffer.c

          meson setup build --cross-file /tmp/cross.txt \
            -Dplatforms=android -Dplatform-sdk-version=26 -Dgallium-drivers=[] -Dvulkan-drivers=wrapper \
            -Dgallium-vdpau=disabled -Dgallium-va=disabled -Dgallium-xa=disabled -Dgallium-opencl=disabled \
            -Ddraw-use-llvm=false -Dglvnd=disabled -Dllvm=disabled -Dshader-cache=disabled -Dvalgrind=disabled \
            -Dbuildtype=release -Dstrip=true
          ninja -C build
          cd -

      - name: 7. Estructurar Selector de Hardware en Carpeta Única /usr/lib/
        run: |
          mkdir -p /tmp/out/usr/lib /tmp/out/usr/share/vulkan/icd.d
          SO_MASTER=$(find wrapper_src/build -name "libvulkan_wrapper.so" -o -name "*.so" | head -n 1)
          cp -f "$SO_MASTER" /tmp/out/usr/lib/libvulkan_wrapper.so || true
          
          # INTERCEPTOR DE ARQUITECTURA NATIVO EN C: Redirige de forma invisible las llamadas de 32 bits a la GPU real del teléfono
          cat << 'EOF' > /tmp/selector.c
          #include <dlfcn.h>
          #include <stddef.h>
          void* vk_icdGetInstanceProcAddr(void* instance, const char* pName) {
              void* handle = NULL;
              if (sizeof(void*) == 8) {
                  handle = dlopen("libvulkan_wrapper.so", RTLD_LAZY | RTLD_LOCAL);
              } else {
                  handle = dlopen("/system/lib/libvulkan.so", RTLD_LAZY | RTLD_LOCAL);
              }
              if (!handle) return NULL;
              void* (*fn)(void*, const char*) = (void* (*)(void*, const char*))dlsym(handle, "vk_icdGetInstanceProcAddr");
              if (!fn) return NULL;
              return fn(instance, pName);
          }
          EOF

          ${ANDROID_SDK_ROOT}/ndk/25.2.9519653/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android26-clang -shared -fPIC -O3 /tmp/selector.c -o /tmp/out/usr/lib/libvulkan.so -ldl

          cat << 'EOF' > /tmp/out/usr/share/vulkan/icd.d/wrapper_icd.aarch64.json
          { "ICD": { "api_version": "1.1.149", "library_path": "libvulkan_wrapper.so" }, "file_format_version": "1.0.0" }
          EOF
          
          cd /tmp/out && tar -cf - usr | zstd -19 > /tmp/wrapper.tzst

      - name: 8. Subir wrapper.tzst Sincronizado
        uses: actions/upload-artifact@v4
        with:
          name: Mesa-Leegao-Wrapper-Original-Unisoc
          path: /tmp/wrapper.tzst
